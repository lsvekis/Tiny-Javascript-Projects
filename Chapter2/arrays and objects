const arr = ['one',5,true,[1,2,3]];
console.log(arr.length);
arr[1] = 100;
arr[50] = 'test';
console.log(arr[1]);
console.log(arr.length);
 
arr.push('end');
let val = arr.pop();
arr.unshift('Start');
let val1 = arr.shift();
 
const arr1 = arr;
console.log(arr1);
